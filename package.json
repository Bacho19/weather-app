{
  "name": "root",
  "private": true,
  "scripts": {
    "start": "lerna run start --parallel",
    "kill-ports": "npx kill-port --port 3000,5000"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "workspaces": {
    "packages": [
      "packages/**"
    ]
  }
}
